Driver Controller:
x ->
y ->
a -> hold

b ->

rb -> Shoot (Hold)
rt ->  Auto Mode: Align to hub (single press), Manual Mode: Nothing

lb -> Begin intaking (toggle)
lt -> Toggle intake in/out

povUp (D-pad) -> (Hold) Winch climber to position
povDown (D-pad) -> (Hold) Winch climber down
povLeft (D-pad) ->
povRight (D-pad) ->

start ->
back ->

Operator Controller:

x ->
y ->
a ->
b ->

rb ->
rt ->

lb ->
lt ->

povUp (D-pad) -> Manual Mode: (hold) Winch climber up
povDown (D-pad) ->  Manual Mode: (hold) Winch climber down
povLeft (D-pad) ->
povRight (D-pad) ->

start ->
back ->
