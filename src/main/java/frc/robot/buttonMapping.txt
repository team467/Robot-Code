Driver Controller:
x ->
y ->
a ->
b ->

rb ->
rt ->

lb ->
lt ->

povUp (D-pad) ->
povDown (D-pad) ->
povLeft (D-pad) ->
povRight (D-pad) ->

start ->
back ->

Operator Controller:

x ->
y ->
a ->
b ->

rb ->
rt ->

lb ->
lt ->

povUp (D-pad) ->
povDown (D-pad) ->
povLeft (D-pad) ->
povRight (D-pad) ->

start ->
back ->
