Driver Controller:
x ->
y ->
a -> (hold) Spin intake and indexer backwards

b ->

rb -> (hold) Shoot 
rt ->  Auto Mode: Align to hub (single press), Manual Mode: Nothing

lb -> (toggle) Begin intaking 
lt -> Toggle intake in/out

povUp (D-pad) ->
povDown (D-pad) -> 
povLeft (D-pad) ->
povRight (D-pad) ->

start ->
back ->

Operator Controller:

x ->
y ->
a ->
b ->

rb ->
rt ->

lb ->
lt ->

povUp (D-pad) -> (hold) Winch climber up
povDown (D-pad) -> (hold) Winch climber down
povLeft (D-pad) ->
povRight (D-pad) ->

start ->
back ->
